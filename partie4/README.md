# Partie 4 - CI/CD

La pipeline est configurée dans `.github/workflows/cicd.yml`.
Elle se déclenche sur chaque push sur la branche `main`.
Le runner self-hosted doit être lancé avec : `cd ~/actions-runner && ./run.sh`
